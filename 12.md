🏗️ PHẦN 1: CẤU TRÚC THƯ MỤC CHUẨN (RESTRUCTURE)
Để hệ thống chạy mượt và dễ mở rộng, bạn nên chia lại các file JS theo từng module nhiệm vụ riêng biệt. Việc dồn hết vào ban_do.js sẽ gây lỗi khi dự án lớn dần.

🗺️ PHẦN 2: CHI TIẾT KẾ HOẠCH TRIỂN KHAI
Giai đoạn 1: Hiển thị & Trực quan hóa (Visualization)
Mục tiêu: Hiển thị chồng xếp các lớp dữ liệu chuẩn xác.

Nhiệm vụ:

Layer Stack: Xếp lớp theo thứ tự: Bản đồ nền (Dưới cùng) -> Vùng quy hoạch (Trong suốt 40%) -> Thửa đất (Đậm màu) -> Ranh giới hành chính (Nét đứt).

Phân loại màu sắc: Tự động đổ màu thửa đất dựa trên mã loai_dat (Ví dụ: ODT - Đỏ, CLN - Vàng).

Giai đoạn 2: Truy vấn & Thông tin (Query & Popup)
Mục tiêu: "Click là có thông tin".

Nhiệm vụ:

Popup nâng cao: Khi click vào thửa đất, không chỉ hiện text mà hiện tab thông tin (Thông tin chung | Pháp lý | Hình ảnh).

Side Panel: Nếu dữ liệu quá nhiều, hiển thị thông tin ở một thanh bên trái (Sidebar) thay vì Popup nhỏ trên bản đồ.

Giai đoạn 3: Phân tích Không gian (The "GIS Power")
Mục tiêu: Trả lời lời câu hỏi "Thửa đất này có dính quy hoạch không?".

Nhiệm vụ:

Overlay Analysis: Sử dụng thư viện Turf.js để tính toán phần giao (intersection) giữa Polygon thửa đất và Polygon vùng quy hoạch.

Tính diện tích thực: Tính diện tích phần bị dính quy hoạch và hiển thị báo cáo nhanh cho Admin.

Giai đoạn 4: Nghiệp vụ Admin & Chỉnh sửa (Admin Tools)
Mục tiêu: Cập nhật biến động đất đai trực tiếp trên Web.

Nhiệm vụ:

Edit Geometry: Sử dụng Leaflet.pm (Geoman) để cho phép Admin nắm kéo các đỉnh (vertex) của thửa đất để chỉnh lại ranh giới.

Split/Merge: Tính năng chia tách thửa đất hoặc hợp thửa (yêu cầu xử lý logic hình học phức tạp).

Save to DB: Tạo nút "Lưu thay đổi" để gửi dữ liệu GeoJSON mới về Server (PostgreSQL/PostGIS).

🛠️ PHẦN 3: YÊU CẦU KỸ THUẬT CHI TIẾT CHO ADMIN
Dưới đây là bảng checklist các tính năng bạn cần tập trung viết code:

💡 GỢI Ý CÁCH LÀM (ARCHITECTURE)
Backend (Quan trọng nhất cho Admin): Bạn nên sử dụng PostgreSQL với Extension PostGIS. Đây là tiêu chuẩn vàng để lưu trữ tọa độ và thực hiện các phép tính giao cắt quy hoạch ở phía Server để đảm bảo độ chính xác.

Frontend Logic: * Tách biệt giữa Chế độ Xem và Chế độ Sửa.

Khi ở chế độ Admin, hãy hiện thêm một thanh công cụ (Toolbar) bên trái với các nút: Vẽ thửa mới, Chỉnh ranh giới, Cập nhật thuộc tính.